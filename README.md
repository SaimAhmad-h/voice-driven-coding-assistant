# voice_enabled_cursor
A voice-enabled coding assistant that converts natural speech into commands to create, edit, and manage code files. It uses an LLM (like Gemini) to understand flexible instructions instead of fixed phrases. The system currently handles file and code operations, with potential to extend into full mouse and system control automation.
